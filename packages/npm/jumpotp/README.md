# JumpOTP

This package selects and runs the prebuilt JumpOTP binary for the current
supported macOS or Linux platform. It has no JavaScript runtime dependencies
and performs no install-time download or compilation.

See [the project documentation](https://github.com/nxxxsooo/jumpotp#readme).
